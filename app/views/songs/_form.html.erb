<h1><%= @song.id ? 'Edit Song' : 'Create Song' %></h1>
<hr/>
<%= form_for [@billboard_song, @song] do |f| %>
  <%= f.label :song %>
  <%= f.text_field :song, required: true %>

  <%= f.label :album %>
  <%= f.text_field :album %>

  <%= f.submit %>
<% end %>
